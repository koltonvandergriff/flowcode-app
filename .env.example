# Supabase (required for real auth — without these, app uses mock auth)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Stripe (optional — billing)
VITE_STRIPE_PRICE_PRO=
VITE_STRIPE_PRICE_TEAM=

# AI Providers (optional — can also be set via Settings or Onboarding)
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...
